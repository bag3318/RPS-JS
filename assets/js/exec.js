// define script variable = to master variable funtion: `rps`
var script = rps();
// run the `init` function inside the `rps` variable function
script.initiate();

// execute the title scroller javascript
scroll.titleScroller();
