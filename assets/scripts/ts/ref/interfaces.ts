interface Rules {
  [index: string]: string;
}
/**
 * interface Gestures {
 *  [index: number]: string;
 * }
 */
interface GameInfo {
  game_name: string;
  version: number;
  creator: string;
  status_ok: boolean;
}